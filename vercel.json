{
    "build": {
        "env": {
            "VITE_API_BASE_URL": "@vite_api_base_url"
        }
    },
    "install": {
        "cmd": "npm install --prefix frontend && npm install --prefix backend",
        "directory": "."
    },
    "projects": [
        {
            "name": "stem-portal-mern-stack",
            "root": "frontend",
            "outputDirectory": "dist"
        }
    ]
}